# Free-claude-code-main
About Free Web Speech API in the browser — no API keys, works on GitHub Pages. Use the bar at the bottom: Listen here — reads main article text from the section you are viewing through the end (not the sidebar TOC) Listen all — reads from section 1 through 77  Resources  
